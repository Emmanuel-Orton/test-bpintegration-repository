package com.bearingpoint.beyond.test-bpintegration.service.defs;

// TODO instead of storing task parameters in every different class, this enum should be used instead, so that we
//  don't duplicate code too much (and we do use same parameters in most of tasks)
public enum TaskParameters {
    OPERATOR_NAME
    ;
}
